#!/usr/bin/python3
"""Represents a class Square with a private instance attribute."""


class Square:
    """Represents a square."""

    def __init__(self, size):
        """Initialize a new Square
    Args:
        size (init): the size of the square.
    """
    self.__size = Size
